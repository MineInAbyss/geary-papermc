package com.mineinabyss.geary.papermc.features.common.event_bridge.entities

/**
 * A helper class for generation completions on the `observe` component.
 * All subclasses' serialNames will be shown in the key name completion.
 */
interface EventBridge